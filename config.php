<?php
declare(strict_types=1);

/**
 * ---------------------------------------------------------------
 *  از AI بپرس (askai) — تنظیمات
 * ---------------------------------------------------------------
 */

return [
    // نام و شعار سایت
    'app_name'    => 'از AI بپرس',
    'app_slogan'  => 'به‌جای جواب دادن، لینک بده!',

    // ساختار لینک نهایی ChatGPT (پرامپت به انتهای آن اضافه می‌شود)
    'chatgpt_url' => 'https://chatgpt.com/?prompt=',

    // حداکثر طول پرامپت
    'max_len'     => 2000,

    // طول کد کوتاه‌کننده لینک
    'code_length' => 6,

    // سرعت تایپ شدن حروف (میلی‌ثانیه بین هر حرف) — عدد بزرگ‌تر = آرام‌تر
    'typing_speed' => 95,

    // ضدّاسپم: حداقل فاصلهٔ زمانی بین دو بار ساخت لینک (ثانیه)
    'rate_limit'   => 4,

    // ضدّاسپم: حداقل زمانی که پر کردن فرم باید طول بکشد (ثانیه)
    'min_fill_time' => 1.2,

    // کلید امضای فرم — حتماً این مقدار را عوض کنید
    'secret'       => 'CHANGE-THIS-SECRET-KEY-9f2b7c1a',

    // پوشهٔ ذخیرهٔ دیتابیس لینک‌ها (باید قابل نوشتن باشد)
    'data_dir'     => __DIR__ . '/data',
];
